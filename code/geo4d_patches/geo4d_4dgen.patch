diff --git a/config/task/inference.yaml b/config/task/inference.yaml
index 4cd4359..d525701 100644
--- a/config/task/inference.yaml
+++ b/config/task/inference.yaml
@@ -57,7 +57,7 @@ shape_meta: &shape_meta
 task_name: &task_name bimanual_video_gen
 dataset_path: &dataset_path [
   # NEEDS OVERWRITE: add episode folders here, for example:
-  /fsx-siro/liuzeyi/4dgen/data/BimanualStoreCerealBoxUnderShelf/2025-03-13T04-19-57+00-00/diffusion_spartan/episode_*
+  /home/sun4208/Geo4D/Data/BimanualPlaceAppleFromBowlIntoBin/2025-03-29T08-08-56+00-00/diffusion_spartan/episode_*
   ]
 
 dataset:
@@ -79,7 +79,7 @@ dataset:
   imagenet_normalization: False
 
   mode: "zarr"
-  replay_buffer_path: /fsx-siro/liuzeyi/4dgen/data/single_task/store_cereal_box_under_shelf/replay_buffer_infer.zarr
+  replay_buffer_path: /home/sun4208/Geo4D/Data/replay_buffer_infer.zarr
 
   compressor: "blosc"
   is_multiarm: true
diff --git a/sgm/modules/attention.py b/sgm/modules/attention.py
index a7428bf..2a29003 100644
--- a/sgm/modules/attention.py
+++ b/sgm/modules/attention.py
@@ -188,10 +188,10 @@ class SelfAttention(nn.Module):
             x = torch.nn.functional.scaled_dot_product_attention(q, k, v)
             x = rearrange(x, "B H L D -> B L (H D)")
         elif self.attn_mode == "xformers":
-            qkv = rearrange(qkv, "B L (K H D) -> K B L H D", K=3, H=self.num_heads)
-            q, k, v = qkv[0], qkv[1], qkv[2]  # B L H D
-            x = xformers.ops.memory_efficient_attention(q, k, v)
-            x = rearrange(x, "B L H D -> B L (H D)", H=self.num_heads)
+            qkv = rearrange(qkv, "B L (K H D) -> K B H L D", K=3, H=self.num_heads)
+            q, k, v = qkv[0], qkv[1], qkv[2]  # B H L D (patched: SDPA)
+            x = torch.nn.functional.scaled_dot_product_attention(q, k, v)
+            x = rearrange(x, "B H L D -> B L (H D)")
         elif self.attn_mode == "math":
             qkv = rearrange(qkv, "B L (K H D) -> K B H L D", K=3, H=self.num_heads)
             q, k, v = qkv[0], qkv[1], qkv[2]  # B H L D
@@ -413,30 +413,8 @@ class MemoryEfficientCrossAttention(nn.Module):
             (q, k, v),
         )
 
-        # actually compute the attention, what we cannot get enough of
-        if version.parse(xformers.__version__) >= version.parse("0.0.21"):
-            # NOTE: workaround for
-            # https://github.com/facebookresearch/xformers/issues/845
-            max_bs = 32768
-            N = q.shape[0]
-            n_batches = math.ceil(N / max_bs)
-            out = list()
-            for i_batch in range(n_batches):
-                batch = slice(i_batch * max_bs, (i_batch + 1) * max_bs)
-                out.append(
-                    xformers.ops.memory_efficient_attention(
-                        q[batch],
-                        k[batch],
-                        v[batch],
-                        attn_bias=None,
-                        op=self.attention_op,
-                    )
-                )
-            out = torch.cat(out, 0)
-        else:
-            out = xformers.ops.memory_efficient_attention(
-                q, k, v, attn_bias=None, op=self.attention_op
-            )
+        # actually compute the attention (patched: torch SDPA, exact same math)
+        out = torch.nn.functional.scaled_dot_product_attention(q, k, v)
 
         # TODO: Use this directly in the attention operation, as a bias
         if exists(mask):
diff --git a/sgm/modules/diffusionmodules/model.py b/sgm/modules/diffusionmodules/model.py
index c904eff..b89aca9 100644
--- a/sgm/modules/diffusionmodules/model.py
+++ b/sgm/modules/diffusionmodules/model.py
@@ -246,9 +246,7 @@ class MemoryEfficientAttnBlock(nn.Module):
             .contiguous(),
             (q, k, v),
         )
-        out = xformers.ops.memory_efficient_attention(
-            q, k, v, attn_bias=None, op=self.attention_op
-        )
+        out = torch.nn.functional.scaled_dot_product_attention(q, k, v)
 
         out = (
             out.unsqueeze(0)
